## 新規実装の野望
### 【UI新規実装】
[ ] シークバー対応: UIは用意せず、ショートカットキー・マウスホイールのみで対応
[ ] プレイリストに「フォルダを開く」を追加、プレイリストフォルダをエクスプローラーで開く
[ ] プレイリストのコピー機能追加

### 【機能新規実装(UIが余り関係しない機能)】
[ ] 音量自動ゲイン
[ ] 背景アートフォルダ(フォトアルバム機能): INIでフォルダ指定
[ ] MP4正式対応: 動画は再生せず、一定時間ごとに動画のそのシーンの静止画を背景アートとして使う
[ ] 画面外移動防止スイッチ: ウィンドウの一部が表示領域をはみ出た場合に引き戻す。マージン設定付き。
[ ] 完全透過時のクリックスルー（操作貫通）機能: 背景透過時にHTTRANSPARENTを返し、UIがない部分の操作を背面へ透過させる

### 【システムトレイ新規実装】
アイデアなし

## 【演出新規実装】
[ ] ボタン系のリアクションを強化
   [ ] ボリュームの場合、マウスホイールを回すことで、アイコンが一瞬拡大するアニメを入れる。
   [x] マウスホバーで音量アイコンもハイライト表示する。
   [ ] ボタン押下時演出のフェード時間、透明度、速度、をINIで調整できるようにする。
[ ] 曲切り替え時の挙動
   [ ] 再生中曲情報のスクロール
   [ ] 背景アートのトランジション
[ ] 新ビジュアライザ
   [ ] 定番スペアナ・グリッドバー表示
   [ ] 時間軸を入れて過去5秒ほどの履歴も見せる「山」モード
   [ ] オーケストラモード: ◯で描いた簡単な人間が多数いて、左から右に扇状に並んでいる。自分の音程に合わせて歌う人数が増える、大合唱モード。
   [ ] FIREWORKS: 過去作自己オマージュ。テキストで作成したパーティクルの花火が音に合わせて打ち上がる。
   [ ] VANGUARDRIFTER：過去作自己オマージュ。ベクターで描いた自機がトレイルを引きながら飛ぶ。
   [ ] PHASE OUT：過去作自己オマージュ。宝石が次々割れていく。
   [ ] STAR BULLET：過去作自己オマージュ。宇宙のチューブを進んでいくレースゲーム風。
   [ ] WORLD SINKs：過去作自己オマージュ。世界が水没する。

### 【既存機能＆演出の調整】
[ ] 音量調節: SHIFTキーを押しながらマウスホイールで1%ずつ変動
[ ] OZアイコンのマウスホバー演出
[ ] プレイリストにアルバムアート表示（いつかやりたい）
[ ] 長文タイトルの電光掲示板（マーキー）スクロール

### 【不具合】
現在特になし

## 現在の仮実装項目（要リファクタリング・置換対象）
後続のステップで本格的な機能に置き換えられた際に、以下の項目は削除または大幅にリファクタリングされる想定です。作業前にここをチェックしてください。

*   **TODO (将来): `LayoutCalculator` の `ConfigManager` 依存の段階的除去**:
    現在、`LayoutCalculator` の各計算メソッドは引数で `const ConfigManager* config` を受け取っている。将来的にこの依存関係を除去し、`Renderer` 側で必要な設定値のみを `LayoutInput` 等の専用構造体へ詰め替え、`LayoutCalculator` が `ConfigManager` を一切知らない完全に独立した形へと移行する。

*    **TODO (将来): Renderer更新メソッドの統合 (Renderer::Update の導入)**:
    現在 Application から個別に呼び出している UpdateAnimation や UpdateTextLayouts などの更新処理を、単一の Renderer::Update(float deltaTime, ...) メソッドへ統合・隠蔽する。これにより Application は Renderer 内部のリソース更新手順を知る必要がなくなり、依存関係をさらにクリーンにする。

*  srcのフォルダ分割
   将来的にWidgetが増えるなら、ファイル名だけでなくカテゴリごとに分けるとAIにも分かりやすい。
   アーキテクチャ資料にはフォルダが何のためにあるか、だけを記載。
   実際においてあるファイルは記載しない。
   具体例：
   Widgets/
   ├── Common/
   │   ├── WidgetCommon.cpp
   │   └── WidgetCommon.h
   ├── Header/
   │   ├── AppLogoWidget.cpp
   │   ├── TrackInfoWidget.cpp
   │   └── NextTrackWidget.cpp
   ├── Controls/
   │   ├── SeekBarWidget.cpp
   │   ├── PlaybackControlsWidget.cpp
   │   └── VolumeControlWidget.cpp
   ├── Playlist/
   │   ├── PlaylistWidget.cpp
   │   ├── PlaylistInput.cpp
   │   ├── PlaylistDraw.cpp
   │   └── PlaylistLayout.cpp
   └── Utility/
      ├── GlobalHotkeysWidget.cpp
      └── ResizeGripWidget.cpp

## 対応済み
### 【UI新規実装】
[x] プレイリストの配置左右オプション化
[x] シャッフルモードON/OFF機能
[x] 右上ボタンの多機能化: ホバー状態で追加アイコンが下(あるいは右)にボタンが出現する
    [x] アプリ終了
    [x] 再生中の曲削除
    [x] プレイリストの表示固定 ※機能は未実装
    [x] ビジュアライザの表示切り替えトグル
    [x] シャッフルモードのON/OFF  ※機能は未実装
    [-] INIファイルのホットロード ※実装見送り
    [x] プレイリストの左右配置切り替え
[x] プレイリスト一覧表示
    [x] プレイリストから、プレイリスト一覧を表示
    [x] プレイリスト新規作成追加
    [x] プレイリスト削除追加
    [x] 再生中の曲をプレイリストから削除するボタン追加
    [x] プレイリスト内の全曲削除
[x] コントロールボタン追加: 10秒巻き戻し、スキップ
[x] スペアナの各種上限自動認識
   [x] 上限Hz
   [x] 音量最大値
[-] スペアナの上限HzのINI持ち: 上記各種上限自動認識が入るまでの暫定措置

### 【機能新規実装(UIが余り関係しない機能)】
[x] 曲情報のプレイリスト保存: 曲再生、あるいは事前読み込みの時点で書き込む
[x] プレイリストの非同期ロード: プレイリストに登録されたファイルのアルバムアート以外を読み込む
[x] INIファイルの即時反映(ホットリロード)
[x] アプリアクティブ化のショートカットキー設定
[x] ビジュアライザ描画ロジックの分離・プラグイン化アーキテクチャの構築 (Phase 19-1)
[x] 事前スキャンによる波形の100%ノーマライズ＆高音域ノイズ足切り機能 (Phase 19-2)
[x] 5バンドEQ（十字アンカー）による動的ダイナミクス調整機能 (Phase 19-2)
[x] メタデータとFFT解析の TrackDatabase / TrackAnalyzer 完全分離 (Phase 19-3)
[x] 保存データの責務分離。oztone_track.odb(OZtoneDB)として、プレイリスト、ファイル情報、アルバムアート設定、フォトアルバム設定、と責務ごとに保存を行う。
[x] INIファイルに不足分があれば自動追記

### 【システムトレイ新規実装】
[x] システムトレイから再生コントロール: 再生、前の曲、次の曲、音量25%・50%・75%・100%(音量はネスト化がいいか)
[x] システムトレイ整理
   [x] 削除：背景モード選択
   [x] 削除：ビジュアライザ選択
   [x] 削除：プレイリスト新規作成
   [x] 削除：プレイリストをクリア
   [x] 削除：リサイズモードを有効化
   [x] 削除：画面位置を固定
[x] プレイリスト管理
    [x] 新規プレイリスト作成: プレイリスト名変更 playlistYYMMDD_HHMM.ozl(連続生成した場合は更にシーケンスを最後につける)
    [x] プレイリスト切り替え: ネスト化する
    [x] プレイリストでネストして、プレイリストフォルダにあるozlファイルを自動認識。システムトレイを開くたびにサーチ。
[-] 再生中の曲をプレイリストから削除 ※プレイリスト上での対応に統合、見送り

### 【演出新規実装】
[x] ボタン系のリアクションを強化
   [-] ボタンにマウスホバーで縁取りを強化。グローを入れてもいいかも。 ※不要と判断、見送り。
   [-] ボタンを押したら沈み込む。影との距離が狭まる。※下記実装により見送り。
   [x] ボタンリアクションとしてボタンの残像を表示。ボタン背景画像は削除。
[x] 曲切り替え時の挙動
   [x] プレイリストで捜査線演出。
[x] 曲ごとの背景アートオフセット:
   [x] アプリがアクティブ時のみカーソル上下左右で背景アートが移動する
   [x] このオフセット値はプレイリストに保存する
[-] 背景アートの「パララックス（視差効果）」 →背景がフィットしてる関係上オミット
[-] 背景アートのフェード設定: 上下左右、フェードサイズをINIで設定する →UIが増えたため違和感が出ると想定、オミット

### 【既存機能＆演出の調整】
   [-] アイコン・テキストの左寄せ ※要検討・様子見
   [x] 現在再生中プレイリスト名の表示
   [x] プレイリストの詳細情報（曲数表示、総プレイ時間HH:MM:SS・プレイリスト内が全てタグ確定してる場合のみ）
[x] プレイリストツールアイコンの整理
   [x] 追加：プレイリスト固定表示
   [-] 追加：シャッフルモード ※シャッフルはプレイリストではなく再生コントロールである、の観点からオミット
[x] ロゴアイコンメニューの整理
   [x] 追加：背景表示切り替え
   [x] 追加：リサイズモード切り替え
   [x] 削除：再生中の曲を削除
   [x] 削除：プレイリスト固定表示 ※機能実装・プレイリストにアイコン追加後
   [-] 削除：シャッフルモード ※要検討 → 再生コントロールとしてロゴメニューに残留決定
   [x] 新規追加：画面固定（位置変更ロック） ※新規追加
[x] OSDを統一：現在OSDに未対応のUIにもOSDを表示
[x] 起動時の画面位置をメインウィンドウ中央に設定
[x] UI要素が何もない背景部分を左or右クリックで、ホバー展開中のUIの格納アニメーションを即座に開始させる。
[x] ヘイローダスト：パーティクル物理演算（命の軌跡）化とサイバーカラーシフト (Phase 19-4)
[x] プリズムビート：純白コアと2重グローによる3パス・リアルネオン化 (Phase 19-5)
[x] ビジュアライザ各種パラメータ（速度、寿命、太さ、透明度など）の完全INIデータ駆動化
[-] 長文タイトル改行対応: 曲名テキストを最大2行で折り返し表示し、増えた高さ分だけ上部のアーティスト名等を上に押し上げる動的レイアウトの実装 →マーキースタイルにするためオミット
[x] 背景アルバムアート操作:右クリック押しっぱなし+ホイールで画像拡縮、右クリック押しっぱなし+ホイールクリックで画像リセット
[x] トラック一覧からプレイリストに戻った時に、再生中プレイリストを「選択状態」にする（そのまま削除対応）

### 【不具合】
[x] 背景透明化失敗: 「リサイズモードを有効化」ON時のみ、背景透過を止める。OFF時は背景透明化。
[x] プレイリスト最後の曲再生時に曲を追加すると先読みを行わない＆次曲に移っても背景＆アート情報が更新されない
[x] MP4除外: 正式対応するまでコメントアウトする
[x] ビジュアライザと音量ボリュームが連動している。元の曲データの音量でいけないか。
[x] ヘイローダストのレーザー演出が画面サイズに連動していない。
[x] システムトレイからの「設定を初期化」時に、自動再起動しない。
   [x] 自動再起動をオミット。全てホットリロードで行う仕様に変更。
[x] アイコンメニューを開いてる途中でマウスがホバー領域から外れると、メニュー展開スライドアニメーションがストップする
[x] 再現条件不明、ロゴアイコンメニューよりプレイリスト（左側設置時）のホバーが優先になるときがある
[x] ボリューム0%にするとビジュアライザが音量0扱いになる → 音量最小値を0.0002fにすることで回避

## グローバルホットキー
WIN+CTRL+ALT+→　：次の曲
WIN+CTRL+ALT+←　：前の曲
WIN+CTRL+ALT+↑　：再生/一時停止
WIN+CTRL+ALT+↓　：停止
WIN+CTRL+ALT+HOME　：ボリュームアップ5%ごと
WIN+CTRL+ALT+END　：ボリュームダウン5%ごと
WIN+CTRL+ALT+PAGEUP　：ボリュームアップ25%ごと
WIN+CTRL+ALT+PAGEDOWN　：ボリュームダウン25%ごと
WIN+CTRL+PAGEUP　：前のプレイリスト
WIN+CTRL+PAGEDOWN　：次のプレイリスト
WIN+CTRL+HOME　：ウィンドウアクティブ（最前面固定＋アクティブ）
WIN+CTRL+END　：ウィンドウアクティブ（最背面固定＋アクティブ）
WIN+CTRL+DELETE　：終了
