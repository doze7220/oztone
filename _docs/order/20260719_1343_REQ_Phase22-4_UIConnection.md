### 作業指示書 REQ: Phase 22-4: UIアーキテクチャの結線（計画立案）
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
2. 以下の【実装要件】を満たすための高度なアーキテクチャ設計と実装計画、兼作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase22-4_UIConnection.md）として新規作成すること。
3. レポートのフォーマットは、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠し、細かなタスクリストを含めること。
4. チャットにて「計画書の作成が完了しました。タスクを実行するための新しいチャットへ移行してください」と報告すること。

#### 【実装要件】
完成したサムネイルエンジン（`ThumbCacher` および `ThumbnailDatabase`）をアプリケーション層およびUIの描画層と結線し、プレイリストの全曲サムネイル表示を稼働させる。

*   **要件1: サムネイル生成キューへのエンキュー (Application層)**
    *   `Application` クラスにて、プレイリストへ楽曲が追加されるタイミング（起動時のリスト復元時、およびドラッグ＆ドロップによる追加時など）で、追加されたファイルパスを `m_thumbCacher.Enqueue()` を用いてキューへ流し込む処理を実装する計画を立てること。
*   **要件2: プレイリストUIへのサムネイル描画 (Widget層)**
    *   `Widget_Playlist_DrawItems.cpp` 等におけるプレイリストの各アイテム（トラック）描画ループ内において、`m_thumbnailDatabase->DrawThumbnail` （または `GetThumbnailBitmap`）を呼び出し、データベースから取得したサムネイル画像を描画するフローへ変更する。
    *   サムネイル画像を描画するにあたり、既存のテキスト（曲名・アーティスト名など）のレイアウトが被らないよう、描画矩形（X座標のオフセット等）を自動調整するロジックを検討すること。

#### 【絶対遵守ルール (Constraints)】
*   **描画ループでの非同期維持**: `Widget` 層の描画ループ内でサムネイルを要求した際、データがキャッシュになくても（まだクックされていなくても）絶対にブロック（待機）してはならない。無い場合はフォールバック（ガラス板など）を即座に描画し、非同期で画像が準備されるのを待つこと。

-------------------------------------------------------------------------------

### 作業指示書 REQ: Phase 22-4 計画書のアーキテクチャ違反修正
* 対象ファイル: D:\ozlab\oztone\_docs\logs\20260719_1343_RES_Phase22-4_UIConnection.md

#### 【作業手順（厳守事項）】
1. 対象ファイル（RES）のタスク4およびタスク5の実装計画について、以下の【指摘事項と解決方針】を読み、アーキテクチャ上のルール違反を自己監査すること。
2. 指摘を理解・納得した場合、対象ファイルを直接編集し、正しいアーキテクチャ（Widgetの完全な受動態化）に沿った確定仕様へとアップデートすること。
3. 作業完了後、チャットにて検討結果の所感と、計画書の更新が完了した旨を報告すること。

#### 【指摘事項と解決方針】
*   **アーキテクチャ違反の指摘:**
    *   タスク4にて「WidgetContext にサムネイル取得用インターフェースを渡す」とあるが、これは Phase 21-9 等で確立した「Widgetの受動態化（データ層への直接アクセスや能動的なデータ取得の禁止）」に完全に違反している。
*   **正しい設計方針 (アンサー):**
    *   **タスク4の修正**: `Renderer_Context.cpp` にて、プレイリストの現在の可視範囲（スクロール位置等）から描画が必要なインデックス範囲を算出し、`ThumbnailDatabase::GetCachedThumbnailBitmap()` （非同期要求とキャッシュ取得）を呼び出す。取得できたビットマップのポインタを `std::unordered_map<size_t, ID2D1Bitmap*>` 等の形で `WidgetContext` へ詰め込み、データとしてWidgetへ渡す設計にすること。
    *   **タスク5の修正**: `Widget_Playlist_DrawItems.cpp` では、インターフェースを呼び出すのではなく、`WidgetContext` から渡されたマップ等を参照し、そこに画像が存在すれば描き、なければフォールバック（ガラス板）を描くだけの純粋な受動態の描画処理に徹すること。

#### 【ファイルの修正要件】
*   対象ファイル内の「タスク4」および「タスク5」の記述を、上記の方針に基づく設計へと断定的に書き換えること。
*   また、タスクを後続で3分割して実行するため、各タスクが「どのファイル」を対象としているかをより明確に記載しておくこと。

-------------------------------------------------------------------------------

### 作業指示書 REQ: Phase 22-4 Task 1-2 : UIアーキテクチャの結線（第1撃：キュー流し込みとレイアウト準備）
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260719_1343_RES_Phase22-4_UIConnection.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはUIアーキテクチャ結線の第1フェーズである。直ちに以下の【実装要件】に従ってコードの修正を実行すること。タスク3以降は後続の指示で行うため絶対に手を触れないこと。
2. 作業完了後、実装したコードと既存の作業レポート（20260719_1343_RES_Phase22-4_UIConnection.md）のタスク1およびタスク2の実装要件を照らし合わせ、反映漏れがないか厳密に自己監査を行うこと。
3. 監査完了後、作業レポートの「タスク1」および「タスク2」のチェックボックスを完了 [x] にし、詳細作業内容を追記すること。
4. チャットにて「タスク1とタスク2が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
*   **タスク1: Application層のエンキュー処理の実装**
    *   対象ファイル: `Application_Initialize.cpp`, `Application_Playlist.cpp`, `Application_FileDrop.cpp` 等
    *   プレイリストへ楽曲が追加される以下のタイミングで、追加されたファイルパスを `m_thumbCacher.EnqueueTrack(filepath)` （メソッド名は ThumbCacher の実装に合わせる）を用いてキューへ流し込む処理を実装する。
        *   起動時のプレイリスト復元時（ロード成功時）
        *   ドラッグ＆ドロップによるファイル追加時
*   **タスク2: LayoutCalculatorの拡張（レイアウト準備）**
    *   対象ファイル: `LayoutCalculator.h`, `LayoutCalculator.cpp`
    *   `PlaylistItemLayout` 構造体に、サムネイル描画用の矩形 `D2D1_RECT_F thumbRect;` を追加する。
    *   `CalculatePlaylistItemLayout` メソッドにて、行の左端（または指定位置）にサムネイル用の領域を計算・確保し、既存のテキスト矩形（タイトル、アーティスト等）がサムネイルと被らないように右へシフト（オフセット）させる計算ロジックを実装する。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳守**: 本REQの目的は「キューへの追加」と「レイアウト計算の準備」のみである。描画処理（RendererやWidget）の変更、および非同期DBの読み出し処理はまだ絶対に記述しないこと。

-------------------------------------------------------------------------------

### 作業指示書 REQ: Phase 22-4 Task 3 : UIアーキテクチャの結線（第2撃：非同期オンデマンド読み出し機構）
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260719_1343_RES_Phase22-4_UIConnection.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはUIアーキテクチャ結線の第2フェーズである。直ちに以下の【実装要件】に従ってコードの修正を実行すること。タスク4以降は後続の指示で行うため絶対に手を触れないこと。
2. 作業完了後、実装したコードと既存の作業レポート（20260719_1343_RES_Phase22-4_UIConnection.md）のタスク3の実装要件を照らし合わせ、反映漏れがないか厳密に自己監査を行うこと。
3. 監査完了後、作業レポートの「タスク3」のチェックボックスを完了 [x] にし、詳細作業内容を追記すること。
4. チャットにて「タスク3が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
*   **タスク3: ThumbnailDatabaseの非同期化インターフェース追加**
    *   対象ファイル: `ThumbnailDatabase.h`, `ThumbnailDatabase.cpp`
    *   UIスレッドを絶対にブロックしないための非同期ロード機構を構築する。
    *   `ID2D1Bitmap* GetCachedThumbnailBitmap(uint32_t thumbId)` を新設する。このメソッドは `m_cache` に画像があれば返し、なければ直ちに `nullptr` を返す（ファイルI/OやWICデコードは絶対に行わない）。
    *   キャッシュミス時に、非同期で画像をロードするメソッド `void RequestThumbnailLoad(uint32_t thumbId, ID2D1RenderTarget* renderTarget, IWICImagingFactory* wicFactory)` （シグネチャは適宜調整可）を実装する。
    *   `RequestThumbnailLoad` 内では、既にロード中（またはキュー入り）のIDを重複して処理しないためのガードを設けた上で、`std::thread` や既存の非同期設計に沿った形で、バックグラウンドスレッドにてパックファイルからの読み出し・WICデコードを行うこと。
    *   デコード後、`m_mutex` 等でスレッドセーフを確保しつつ、`ID2D1Bitmap` を生成して LRU キャッシュ (`m_cache` および `m_lruList`) へ登録するロジックを完成させること。

#### 【絶対遵守ルール (Constraints)】
*   **UIスレッドの保護**: `GetCachedThumbnailBitmap` 内では絶対にファイルアクセスやWICデコード等の重い処理を行わないこと。
*   **COMマルチスレッドとリソース生成の安全性**: 別スレッドでD2Dリソース（`ID2D1Bitmap`）を生成する際、対象のレンダ―ターゲットやファクトリがマルチスレッド対応（`D2D1_FACTORY_TYPE_MULTI_THREADED`）であることを前提とした安全な実装を行うこと。
*   **スコープの厳守**: 本REQの目的は「非同期ロード機構の構築」のみである。UI（Widget層やRenderer層）への結線は次のフェーズで行うため、まだ書き換えないこと。

-------------------------------------------------------------------------------

### 作業指示書 REQ: Phase 22-4 Task 4-5 : UIアーキテクチャの結線（第3撃：レンダラからの取得と受動態描画）
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260719_1343_RES_Phase22-4_UIConnection.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはUIアーキテクチャ結線の最終フェーズである。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、実装したコードと既存の作業レポート（20260719_1343_RES_Phase22-4_UIConnection.md）の実装要件を照らし合わせ、反映漏れがないか厳密に自己監査を行うこと。
3. 監査完了後、作業レポートの「タスク4」および「タスク5」のチェックボックスを完了 [x] にし、詳細作業内容を追記すること。
4. チャットにて「Phase 22-4の全タスクが完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
*   **タスク4: Renderer層での可視範囲計算とWidgetContextへの結線**
    *   対象ファイル: `WidgetContext.h`, `Renderer_Update.cpp` (または更新系ファイル), `Renderer_Context.cpp`
    *   `WidgetContext.h` にサムネイル画像を渡すためのお弁当箱として `std::unordered_map<size_t, ID2D1Bitmap*> playlistThumbnails;` を追加する。
    *   `Renderer_Context.cpp` 等にて、プレイリストの現在の可視範囲（スクロール位置や表示可能アイテム数等から算出）に存在するインデックス範囲を割り出す。
    *   算出した範囲の各インデックスに対して `ThumbnailDatabase::GetCachedThumbnailBitmap()` を呼び出し、画像がなければ `ThumbnailDatabase::RequestThumbnailLoad()` で非同期ロードを要求する。
    *   取得できたビットマップのポインタを `playlistThumbnails` マップへ格納し、Widgetへ渡す準備を整える。
*   **タスク5: Widget_Playlist_DrawItemsの純粋な描画対応**
    *   対象ファイル: `Widget_Playlist_DrawItems.cpp`
    *   アイテム描画ループ内にて、データ層のインターフェースを直接叩くのではなく、コンテキストから渡された `ctx.playlistThumbnails` のマップを参照する。
    *   対象インデックスの画像が存在すれば描画し、存在しなければフォールバック（ガラス板等）を描画する。
    *   画像やガラス板を描画する際、LayoutCalculator が事前に確保した `thumbRect` の領域に美しく収まるように描画すること。

#### 【絶対遵守ルール (Constraints)】
*   **Widgetの完全な受動態化**: Widget層（`Widget_Playlist_DrawItems.cpp`）では絶対に `ThumbnailDatabase` 等のデータ層へ直接アクセスしたり、メソッドを呼び出したりしないこと。必要なデータは必ず Renderer が `WidgetContext` に詰めて一方的に渡すこと。
*   **非ブロック描画の維持**: キャッシュに画像がない場合でも、絶対に描画ループを停止（ブロック）させないこと。
