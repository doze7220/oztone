### 作業指示書 REQ: Hotfix: ボリューム0%時のビジュアライザ停止回避 (実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはHotfixの「実装実行」である。事前のレポート作成や計画立案は不要なので、直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. コード修正が完全に終わった後、Hotfix作業レポートテンプレートに従い、作業レポート（`D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Hotfix_VolumeZeroVisualizer.md`）として新規作成し、原因（0除算回避による波形処理スキップ）と対応内容（極小値によるクランプ偽装）を追記すること。
3. チャットにて「ボリューム0%時のビジュアライザバグ修正が完了しました。ビルド・動作確認をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク1: AudioPlayer::SetVolume における極小値クランプの導入**
    *   `src/AudioPlayer.cpp` の `AudioPlayer::SetVolume(float volume)` メソッドを改修する。
    *   引数として渡された `volume` を `0.0f` 〜 `1.0f` の範囲でクランプしている箇所（またはそのまま設定している箇所）において、下限値を `0.0f` ではなく `0.0001f` （完全な無音に聞こえるがゼロではない極小値）に変更してクランプ・適用する。
    *   これにより、ユーザーがUI上でボリュームを0%にした場合でも、内部的には `GetVolume()` が `0.0001f` を返すようになり、`ProcessAudioFrames` 内の波形逆算処理（`volume > 0.0f` の条件）が実行され続け、ビジュアライザの波形が停止しないようにする。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳守**: 本作業は `AudioPlayer::SetVolume` における下限値の変更のみに留め、ビジュアライザ本体やUIのパーセンテージ描画処理などには一切干渉しないこと。
-----------------------------------------------------------------------------------
### 作業指示書 REQ: Hotfix: ボリューム0%時のビジュアライザ停止回避 鉄壁ガード版 (実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはHotfixの「実装実行」である。事前のレポート作成や計画立案は不要なので、直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. コード修正が完全に終わった後、Hotfix作業レポートテンプレートに従い、作業レポート（`D:\ozlab\oztone\_docs\logs\20260715_0122_RES_Hotfix_VolumeZeroVisualizer.md`）に、`### 6. 追加対応`として原因（マイナス値の貫通）と対応内容（std::clampによる厳格なガード）を追記すること。
3. チャットにて「ボリュームマイナス値貫通バグの修正が完了しました。ビルド・動作確認をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク1: AudioPlayer::SetVolume における厳格なクランプの適用**
    *   `src/AudioPlayer.cpp` の `AudioPlayer::SetVolume(float volume)` メソッドを改修する。
    *   現在、マイナス値（-0.05fなど）が渡された際に下限ガードがすり抜けて波形処理が停止するバグが発生している。これを完全に防ぐため、メソッドの先頭付近で必ず `volume = std::clamp(volume, 0.0001f, 1.0f);` を用いて、いかなる値が来ても範囲内に収めるよう修正すること。（必要に応じて `<algorithm>` をインクルードすること）
    *   これにより、ホイールを下へ回し続けてマイナス値が連続で投げ込まれても、内部的には常に `0.0001f` に維持され、`volume > 0.0f` の波形処理ループが絶対に停止しないようにする。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳守**: 本作業は `AudioPlayer::SetVolume` におけるクランプ処理の厳格化のみに留めること。
