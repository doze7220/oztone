【AIへの指示（Phase 5 - Step 1 : PlaylistManagerの実装とフルパス再生）】
現在のプロジェクトをベースに、次の実装を開始します。

**【今回の目的：Phase 5 - Step 1】**
再生待ちキューを管理する `PlaylistManager` クラスを新設し、テスト用のMP3を「フルパス」でリストに登録して再生する仕組みを構築します。

**【実装要件】**
1. **PlaylistManager クラスの新設**:
   - `src/PlaylistManager.h`, `src/PlaylistManager.cpp` を作成してください。
   - `std::vector<std::string>`（または `std::wstring`）を用いて、曲のファイルパスをリスト（キュー）として保持できるようにしてください。
   - 現在の再生インデックスを管理する変数（`m_currentIndex` 等）を持たせてください。
   - リストへの追加 (`Add`)、現在の曲のパス取得 (`GetCurrentTrack`) などの基本的なメソッドを実装してください。

2. **Application の拡張 (フルパスの解決とPlaylistManagerの統合)**:
   - `Application` クラスのメンバに `PlaylistManager` を追加してください。
   - `Application::Initialize` 内で、現在の実行ファイル (`OZtone.exe`) のパスを取得し、そこから `<filesystem>` 等を用いてプロジェクトルートの `assets/test.mp3` への **絶対パス（フルパス）** を生成してください（※exeは `build/Debug/` 等に出力されるため、ディレクトリ階層を遡る必要があります）。
   - 生成したフルパスを `PlaylistManager` に追加し、そこから `GetCurrentTrack` でパスを取り出して `TagManager` および `AudioPlayer` に渡して再生するようにロジックを変更してください。

3. **CMakeLists.txt の整理**:
   - `test.mp3` をプロジェクト直下の `assets` から直接フルパスで読み込むようになったため、`CMakeLists.txt` 内の `POST_BUILD` で行っていた `test.mp3` を出力ディレクトリへコピーする処理を削除してください。
   - 新設した `PlaylistManager` のソースとヘッダをビルド対象に追加してください。

**【事後処理】**
実装完了後、`.\build.bat` を実行して自動ビルドテストを行ってください。エラーがなければ、デバッグ実行等で `test.mp3` がフルパス経由で正しく再生・表示されるか確認し、成功したら `_docs/logs/YYYYMMDD_HHMM_Phase5_Step1.md` へ実装レポートを出力してください。
