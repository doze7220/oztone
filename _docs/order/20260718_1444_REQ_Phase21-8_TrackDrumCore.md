### 作業指示書 REQ: Phase 21-8: トラックドラム (Track Drum) コアエンジンの実装 (計画立案)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
2. 以下の【実装要件】を満たすための高度なアーキテクチャ設計と実装計画、兼作業レポート（`YYYYMMDD_HHMM_RES_Phase21-8_TrackDrumCore.md`）として新規作成すること。
3. レポートのフォーマットは、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠し、細かなタスクリストを含めること。
4. チャットにて「計画書の作成が完了しました。タスクを実行するための新しいチャットへ移行してください」と報告すること。

#### 【実装要件】
曲の切り替え時（メディアキー等による Next / Prev 時）に、物理的な「質量」と「慣性」を持ったドラムが回転するトランジションUI（トラックドラム）のコアエンジンを実装する。本機能はINIで有効/無効を切り替え可能とし、無効時（従来モード）はアニメーションを行わず即時切り替えを保証する。
※マウスホイール連動やUIのホバーアフォーダンス等は後続フェーズ(Phase 21-9)で実装するため、本フェーズでは**「描画とバケツリレーの物理演算」**のみにスコープを限定する。

*   **要件1: ConfigManagerの拡張 (`[TrackDrum]` セクション)**
    *   INIファイルに `[TrackDrum]` セクションを新設し、以下のパラメータを管理する。
    *   `EnableTrackDrum` (デフォルト: 1): 機能の有効/無効トグル。0の場合は従来通りの即時切り替えとする。
    *   `MaxDuration` (デフォルト: 3.0): 最長アニメーション時間（秒）。
    *   `MinSpeed` / `MaxSpeed` (デフォルト: 10.0 / 60.0): 動的変化する最低・最高回転速度（スロット/秒）。
    *   `Acceleration` / `Deceleration` (デフォルト: 20.0 / 15.0): ドラムの初速加速度と停止減速度。

*   **要件2: トラックドラムの描画とバケツリレー管理 (Renderer / Widget_TrackInfo 等)**
    *   描画オブジェクトは「消えゆく曲（OLD）」と「現れる曲（NOW）」の2つだけでバケツリレーを行う。
    *   `UpdateAnimation` にて、加速度・減速度を用いて速度をS字カーブで計算し、インデックスを更新する。
    *   遠距離ジャンプ（シャッフル時やプレイリストからの直接ジャンプ等）が入力された場合は、現在の速度と `MaxDuration` から最大表示可能数を割り出し、間のインデックスを間引き（スキップ）して描画を追いつかせる。
    *   超高速回転中は重い画像デコードを行わず、半透明のガラス板（`FallbackArtOpacity` の流用など）を描画。目標地点に着地した瞬間に正規アートの非同期ロードを開始し、完了次第クロスフェードで出現させる。

*   **要件3: シャッフル時および既存操作の安全設計**
    *   静止時にシャッフルが切り替わった場合は表示を動かさず裏のリストのみ更新。アニメーション中の場合はOLD/NOWをそれぞれ着地させてから新リストを引き込む。

#### 【作業終了後】
1. 作業レポート（`YYYYMMDD_HHMM_RES_Phase21-8_TrackDrumCore.md`）に、詳細作業内容を記載する（タスクリストに含める）こと。
2. D:\ozlab\oztone\PROJECT_ARCHITECTURE.md を確認し、作業内容が影響する場合は修正を行う（タスクリストに含める）こと。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳守**: マウスホイール操作の検知やホバーアフォーダンスの実装は絶対に行わないこと。
*   **フォールバックの絶対保証**: `EnableTrackDrum` が 0 に設定された場合は、一切の回転アニメーションを行わず即座に次の曲を描画する従来の挙動を完全に維持すること。

-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8: トラックドラム (Track Drum) コアエンジンの実装 (計画修正)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
2. 前回作成した計画書（`YYYYMMDD_HHMM_RES_Phase21-8_TrackDrumCore.md`）の設計には、アーキテクチャを破綻させる重大な見落とし（背景アートの非同期、非同期画像ロードの責務違反）が存在する。以下の【実装要件】および【絶対遵守ルール】を熟読し、設計とタスクリストを完全に修正した新しい計画書として再出力（上書き）すること。
3. レポートのフォーマットは、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠し、細かなタスクリストを含めること。
4. チャットにて「計画書の修正が完了しました。内容を確認後、タスクを実行するための新しいチャットへ移行してください」と報告すること。

#### 【実装要件】
曲の切り替え時（メディアキー等による Next / Prev 時など）に、物理的な「質量」と「慣性」を持ったドラムが回転するトランジションUI（トラックドラム）のコアエンジンを実装する。本機能はINIで有効/無効を切り替え可能とし、無効時（従来モード）は即時切り替えを保証する。
※マウスホイール連動やUIのホバーアフォーダンス等は後続フェーズで実装するため、本フェーズでは**「描画とバケツリレーの物理演算」**のみにスコープを限定する。

*   **要件1: ConfigManagerの拡張 (`[TrackDrum]` セクション)**
    *   `EnableTrackDrum` (デフォルト: 1)、`MaxDuration` (3.0)、`MinSpeed`/`MaxSpeed` (10.0/60.0)、`Acceleration`/`Deceleration` (20.0/15.0) の設定管理を追加。

*   **要件2: トラックドラムの描画とバケツリレー管理**
    *   描画オブジェクトは「消えゆく曲（OLD）」と「現れる曲（NOW）」の2つだけでバケツリレーを行う。
    *   速度をS字カーブ等で計算し、`MaxDuration` 制約に基づき遠距離ジャンプ時のインデックス間引き（スキップ）を行う。
    *   超高速回転中は半透明のガラス板（`FallbackArtOpacity` の流用など）を描画し、着地した瞬間にクロスフェードで出現させる。

*   **要件3: シャッフル時および既存操作の安全設計**
    *   静止時にシャッフルが切り替わった場合は表示を動かさず裏のリストのみ更新。アニメーション中の場合はOLD/NOWをそれぞれ着地させてから新リストを引き込む。

#### 【作業終了後】
1. 作業レポート（`YYYYMMDD_HHMM_RES_Phase21-8_TrackDrumCore.md`）を修正し、詳細作業内容を記載する（タスクリストに含める）こと。
2. D:\ozlab\oztone\PROJECT_ARCHITECTURE.md を確認し、作業内容が影響する場合は修正を行う（タスクリストに含める）こと。

#### 【絶対遵守ルール (Constraints) - 修正指示】
*   **背景アートとの完全同期 (スコープ問題の解決)**: 左下の `TrackInfoWidget` のみでドラム回転を完結させてはならない。背景アート（`DrawBackground`）もドラム回転のアニメーション（バケツリレー・クロスフェード）と完全に同期させること。必要であれば、背景描画の責務も `Widget_TrackInfo` へ統合・吸収するか、`Renderer` のコンテキストで状態を一括管理する設計へと修正すること。
*   **非同期画像ロードの責務厳守 (マルチスレッド暴走リスクの回避)**: `Widget_TrackInfo` 等のUIコンポーネント内で、独自に `std::thread` や `std::async` を立てて画像読み込み（ファイルI/O）を絶対に行わないこと。画像のロードは、必ず `Application` 層の既存の先読み機構、または `TrackDatabase` / `TrackAnalyzer` と連携する形を取ること。Widgetは「Application層から画像が渡されるまでガラス板を描画して待つ」という受動的な設計とし、必要に応じて `Application_*.cpp` へのデータ連携・ロード指示処理をタスクに追加すること。
*   **スコープの厳守**: マウスホイール操作の検知やホバーアフォーダンスの実装は絶対に行わないこと。
*   **フォールバックの絶対保証**: `EnableTrackDrum` が 0 の場合は、一切のアニメーションを行わず即座に次の曲を描画する従来の挙動を完全に維持すること。

-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Task 1 : ConfigManagerの拡張
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム設定パラメータの追加である。直ちに以下の【実装要件】に従ってコードとドキュメントの修正を実行すること。
2. 作業完了後、既存の作業レポート（20260718_1445_RES_Phase21-8_TrackDrumCore.md）の「タスク1」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「ConfigManagerの拡張(Phase 21-8 Task 1)がすべて完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
トラックドラム機能のパラメータをINIファイルで管理するため、ConfigManagerを拡張する。

*   **要件1: [TrackDrum] セクションの追加**
    *   `ConfigManager.h`, `ConfigManager_Window.cpp` (または適切な実装ファイル), `ConfigManager_DefaultIni.h` を修正し、以下の設定値の読み書き処理とゲッターを追加する。
    *   `EnableTrackDrum` (デフォルト: 1)
    *   `MaxDuration` (デフォルト: 3.0)
    *   `MinSpeed` (デフォルト: 10.0)
    *   `MaxSpeed` (デフォルト: 60.0)
    *   `Acceleration` (デフォルト: 20.0)
    *   `Deceleration` (デフォルト: 15.0)

#### 【絶対遵守ルール (Constraints)】
*   **SSOTの厳守**: 設定の初期値は必ず `ConfigManager_DefaultIni.h` 内の `DEFAULT_INI_CONTENT` にのみ記述し、C++のコード内で初期値をハードコード（二重管理）しないこと。
*   **スコープの厳守**: 本タスクでは `Widget_TrackInfo` や `Renderer` の描画ロジックの修正は絶対に行わないこと。


-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Task 2 : トラックドラムの状態変数と物理演算エンジンの追加
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラムのコアとなる物理演算および状態管理の追加である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、既存の作業レポート（20260718_1445_RES_Phase21-8_TrackDrumCore.md）の「タスク2」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「状態変数・物理演算の追加(Phase 21-8 Task 2)がすべて完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
トラックドラムの回転アニメーションを処理するための状態管理と、バケツリレー（OLD/NOW）の物理演算エンジンを構築する。

*   **要件1: 状態変数の追加 (同期管理設計)**
    *   `Widget_TrackInfo.h/cpp` または `Renderer_Context.cpp` / `WidgetContext` 等に、トラックドラムの進行状態（位置、速度、目標インデックス等）と、OLD/NOWのアート・テキスト情報を保持するメンバ変数を追加する。
    *   ※後のタスクで「背景アート (`DrawBackground`)」もこの状態を参照して同期回転・クロスフェードするため、背景描画からもアクセスしやすい設計（またはRenderer側での一括管理）とすること。

*   **要件2: バケツリレーと物理演算の実装**
    *   曲が変更された際、現在の表示内容を「OLD」へ退避し、新しい情報を「NOW」として設定するバケツリレー機構を実装する。
    *   `ConfigManager` から取得した `Acceleration`, `Deceleration`, `MaxSpeed` 等を用いて、S字カーブ等での速度計算および位置更新ロジックを実装する（実際の画像デコードや描画処理の詳細なクロスフェードは後続タスクで行うため、まずは「状態の更新ロジック」に集中すること）。

*   **要件3: フォールバック（従来挙動）の保証**
    *   `EnableTrackDrum` が 0（無効）の場合は、一切のアニメーション・バケツリレー処理を行わず、即座に「NOW」に情報をセットして旧仕様と同じ即時切り替えを行うようにフォールバックロジックを実装する。

#### 【絶対遵守ルール (Constraints)】
*   **非同期ロードの禁止**: 本タスク内で `std::thread` などを立てて独自の画像ロード（ファイルI/O）を行うことは絶対に禁止する。
*   **スコープの厳守**: 今回は「状態管理と数値計算（更新ロジック）」の追加に留め、マウスホイールの入力判定などは追加しないこと。

-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Task 3 : 遠距離ジャンプのスキップ処理の実装
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラムの物理演算における「間引き（スキップ）処理」の追加である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、既存の作業レポートの「タスク3」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「遠距離ジャンプのスキップ処理(Phase 21-8 Task 3)がすべて完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
プレイリストからの直接ジャンプ等により、OLDとNOWのインデックス距離が大きく離れている場合、設定された `MaxDuration` の時間内にアニメーションが確実に完了するように、仮想的な回転距離（スロット数）を間引くロジックを `Renderer::UpdateAnimation`（または `SetTrackInfo` 内の初期化時）に実装する。

*   **要件1: スロット距離の算出と上限制限**
    *   `m_drumTargetIndex` と現在の `m_drumPosition`（またはOLDのインデックス）から、目標までの移動距離（スロット数）を算出する。
    *   `ConfigManager` の `TrackDrumMaxSpeed` と `MaxDuration` を掛け合わせた値が「物理的に表示可能な最大スロット数」となる。
    *   移動距離がこの最大表示可能数を超過する場合、間のインデックスを仮想的にスキップ（移動距離を最大スロット数に丸める等）し、必ず `MaxDuration` 秒以内に目標地点へ着地できるように計算を補正する。

*   **要件2: 既存物理演算への統合**
    *   Task 2で実装した速度（Velocity）や位置（Position）の更新ロジックにこの制限を組み込み、遠距離でもシームレスに加速・最高速維持・減速（着地）が行えるようにする。

#### 【絶対遵守ルール (Constraints)】
*   **描画ロジックの変更禁止**: 本タスクは「物理演算（数値の更新）」のみに集中すること。高速回転中のガラス板描画やクロスフェードなどのUI描画処理は Task 4 以降で行うため、今回は絶対に触れないこと。
*   **非同期I/Oの禁止**: 画像ロードなどの独自スレッド実装は絶対に行わないこと。

-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Task 4 : ガラス板描画と非同期画像ロードの連携
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラムにおける「フォールバック描画（ガラス板）と非同期ロード連携」の実装である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、既存の作業レポートの「タスク4」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「ガラス板と非同期ロードの実装(Phase 21-8 Task 4)がすべて完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
高速回転中は重い画像デコードをスキップして「半透明のガラス板」を描画し、着地時に非同期で画像をロードしてクロスフェードで出現させる処理を実装する。

*   **要件1: 描画ロジックの適用 (Renderer_Draw.cpp / Widget_TrackInfo.cpp 等)**
    *   Task 2, 3 で計算された `m_drumPosition` や `m_isDrumAnimating` を用いて、実際の描画座標（オフセット）やアルファ値（不透明度）を計算する。
    *   高速回転中（目標インデックスに未到達、または画像未ロード時）は、実際の画像ではなく `ConfigManager` で定義された `FallbackArtOpacity` 等を用いた半透明の矩形（ガラス板）を描画する。
    *   目標インデックスに着地し、かつ画像がロード完了したタイミングで、ガラス板から正規アートへとクロスフェードさせるロジックを組み込む。

*   **要件2: Application層での非同期ロード連携 (Application_Playback.cpp等)**
    *   トラックドラムが目標インデックスに着地（または着地直前）したことを検知したタイミングで、`Application` 層の既存の非同期読み込み機構（`PrefetchNextTrack` 相当の処理）をトリガーし、必要な画像をバックグラウンドでロードする。
    *   Widget 側で独自の `std::thread` 等は絶対に立てず、Application層から画像が渡されるまでガラス板を描画して待つという受動的な設計を厳守する。

#### 【絶対遵守ルール (Constraints)】
*   **描画とロードの責務分離**: 非同期でのファイルI/Oは `Application` 層で行い、`Renderer` や `Widget` は状態に応じた描画のみを行うこと。
*   **フォールバックの保証**: `EnableTrackDrum` が 0 の場合は、これらのアニメーション処理を行わず即時切り替えを維持すること。
-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Hotfix : トラックドラム描画の完全ブラッシュアップ
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム実装におけるUI描画バグの修正（Hotfix）である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、作業レポート（`20260718_1445_RES_Phase21-8_TrackDrumCore.md` 等の関連ファイル）の末尾に Hotfix 項目を追加し、対応内容を記録すること。
3. チャットにて「トラックドラムの描画Hotfixが完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
トラックドラム（プレイドラム）機能において報告された4つのUIバグを修正し、トランジションの品質を極限まで高める。

*   **要件1: 描画のクリッピングの適用**
    *   `Widget_TrackInfo`（および必要であれば背景描画）において、OLD/NOWのスライドアニメーションが描画領域外にはみ出さないよう、描画ブロックを `PushAxisAlignedClip` と `PopAxisAlignedClip` で挟み込み、正確にマスク処理を行うこと。
*   **要件2: CD帯UIの追従とインデックスバケツリレー**
    *   CD帯（トラックナンバー）の描画座標に対しても、ドラムの移動オフセット（`m_drumPosition` 等の変動値）を適用し、アルバムアートと一緒にスライド・回転するように修正すること。
    *   バケツリレー状態変数に `m_oldTrackIndex` 等（本来のシャッフルインデックスを引くための情報）を追加し、OLD側として消えゆくCD帯にも正しいトラックナンバーが表示されるようにすること。
*   **要件3: ガラス板の「抜け」の防止**
    *   高速移動中やスキップ処理中に画像がロードされていないスロットが存在する場合、何も描画されない（抜ける）ことを防ぐため、必ず `FallbackArtOpacity` などを利用した「半透明の黒板（ガラス板）」の矩形描画フォールバックが働くように条件分岐を修正すること。
*   **要件4: 先読み済み画像の維持（隣接曲のシームレス化）**
    *   次/前の曲への移動時など、**既に画像がメモリ上にロードされている（先読み済み等で利用可能である）場合**は、アニメーション中であってもガラス板に差し替えず、そのまま正規のアルバムアート画像を用いてスライド・回転を描画すること。ガラス板へのフォールバックは「画像データが手元に無い（非同期ロード待ち）」の場合のみに限定すること。

#### 【絶対遵守ルール (Constraints)】
*   既存の物理演算ロジック（速度やS字カーブ、間引き計算）自体は正常に機能しているため変更しないこと。あくまで「描画条件と座標への適用」にスコープを絞ること。
-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Hotfix 2 : 仮想スロット描画への完全改修
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム描画エンジンの致命的なバグ修正（Hotfix）である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、作業レポートの末尾に Hotfix 項目を追加し、対応内容を記録すること。
3. チャットにて「トラックドラムの仮想スロット描画化が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
`Widget_TrackInfo.cpp` 内のドラム描画ロジックを、OLD/NOWの2要素固定描画から「現在のスクロール位置に基づく仮想スロット描画」へとアーキテクチャを根本から改修し、3つのバグを修正する。

*   **要件1: クリッピング領域の厳格化**
    *   `PushAxisAlignedClip` に渡すクリッピング矩形の高さを、テキスト領域を含まない **「アルバムアートの高さ (`layout.artSize`)」** に限定するように修正する。これにより、ドラムの上下はみ出しを完全に切り落とす。
*   **要件2: 仮想スロットループ描画の導入（中間スロット抜けの解消）**
    *   OLDとNOWの2つだけを描画するのではなく、現在の `m_drumPosition` を基準に、画面内に表示されるべきインデックス（`floor(m_drumPosition)` から `ceil(m_drumPosition)` など）をループしてスロットを描画するロジックに変更する。
    *   描画対象のスロットのインデックスが `m_oldTrackIndex` と一致する場合は、OLDの画像（`m_oldArtBitmap`）とOLDのテキストを描画する。
    *   描画対象のスロットのインデックスが `m_drumTargetIndex` と一致する場合は、NOWの画像（`ctx.currentArtBitmap`）とNOWのテキストを描画する。
    *   **どちらにも一致しない「中間のスロット」の場合は、`FallbackArtOpacity` を用いた半透明のガラス板矩形と、そのスロットに応じたCD帯（本来のトラックナンバー）のみを描画して流す。**
*   **要件3: フォールバック（ガラス板）判定の純化と先読み画像の維持**
    *   OLDおよびNOWを描画する際、「アニメーション中かどうか」でガラス板に差し替えるのではなく、**「画像データ（`ID2D1Bitmap*`）が `nullptr` かどうか」のみでガラス板か正規画像かを判定**するように純化する。これにより、OLDが突然消えるバグや、先読みされた画像がスクロール完了まで表示されないバグを解消する。

#### 【絶対遵守ルール (Constraints)】
*   本修正は `Widget_TrackInfo.cpp` の `Draw` などの描画メソッド内に閉じて行うこと。物理演算ロジック（`Renderer::UpdateAnimation`）には触れないこと。
-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Hotfix 2-2 : ドラムスクロール距離の固定化
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム描画バグの修正（Hotfix）の第2段階である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、作業レポートの末尾に Hotfix 項目を追加し、対応内容を記録すること。
3. チャットにて「スクロール距離の固定化(Hotfix 2-2)が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
`Widget_TrackInfo.cpp` にて、高さが足りない（長方形の）アルバムアートが描画された際、ドラムのスクロール位置がズレてしまうバグを修正する。

*   **要件1: スクロール単位（スロット間隔）の画像サイズ依存排除**
    *   ドラムアニメーション時のY座標スライド量（オフセット）の計算において、画像自体の実際のサイズ（`GetSize()` 等）や、フィット計算によって生じる余白（`offsetY` 等）が、**スクロールの移動距離自体に影響を与えていないか**を確認し、分離する。
    *   ドラムの1スロット分の物理的な移動距離は、画像の縦横比やサイズに関わらず、必ず `LayoutCalculator` によって算出された **`layout.artSize` (アルバムアートの正方形の枠サイズ)** を基準とした絶対的な固定値とするように計算式を修正する。
    *   画像の中央揃え描画は、あくまで「その固定されたスロット（枠）の内部」でのみ完結するように描画オフセットを適用すること。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳密な限定**: 本タスクは「スクロール時のY座標移動量（オフセット計算）の固定値化」のみにスコープを限定する。仮想スロットループ描画等のロジック変更は行わないこと。
-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Hotfix 2-3 : 中間スロットのテキスト情報補完
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム描画バグの修正（Hotfix）である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、作業レポート（`20260718_1445_RES_Phase21-8_TrackDrumCore.md`）の末尾に Hotfix 項目を追加し、対応内容を記録すること。
3. チャットにて「中間スロットのテキスト情報補完(Hotfix 2-3)が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
`Widget_TrackInfo.cpp` にて、遠距離ジャンプ（複数曲跨ぎ）のトラックドラムアニメーション中に、中間スロットの曲名およびアーティスト名が描画されない不具合を修正する。

*   **要件1: 中間スロットへのメタデータテキスト描画の追加**
    *   仮想スロットループ描画において、OLDおよびNOW「以外」の中間スロットを描画する際、ガラス板とCD帯に加えて、**「曲名（タイトル）」および「アーティスト名」** も描画するように処理を追加する。
    *   描画対象スロットのインデックスから、`WidgetContext` が保持している `shuffleIndices` およびメタデータのリスト（`TrackMetadata` 群）を参照して正しい文字列を取得すること。
    *   取得した文字列を用いて一時的な `IDWriteTextLayout` を生成し（または適切な描画ユーティリティを使用し）、OLD/NOWを描画する際と全く同じフォント、サイズ、マージン、ドロップシャドウ等のスタイルでガラス板の上にテキストを描画して流すこと。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳密な限定**: 本タスクは「中間スロット描画時のテキスト描画処理の追加」のみにスコープを限定する。スクロール距離の計算ロジックや画像の非同期ロードロジックには触れないこと。
-----------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 21-8 Hotfix 2-4 : フェードインとガラス板判定の純化
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラム描画バグの修正（Hotfix）である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、作業レポート（`20260718_1445_RES_Phase21-8_TrackDrumCore.md`）の末尾に Hotfix 項目を追加し、対応内容を記録すること。
3. チャットにて「フェードインとガラス板判定の純化(Hotfix 2-4)が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
`Widget_TrackInfo.cpp` にて、アニメーション完了（着地）時に、既に画像が存在しているにも関わらず強制的にフェードイン処理が走り、アルバムアートが一瞬消えてしまう不具合を修正する。

*   **要件1: ガラス板フォールバック判定の純化**
    *   OLD、NOW、および中間スロットを描画する際、「アニメーション中かどうか」でガラス板に差し替えるのではなく、**「画像データ（`ID2D1Bitmap*`）が `nullptr` かどうか」のみで**ガラス板か正規画像かを判定するように条件を純化する。
    *   これにより、隣の曲など先読み（Prefetch）されて既に画像がメモリ上にある場合は、スクロール中であってもガラス板ではなく最初から正規のアルバムアートを描画してスライドさせる。

*   **要件2: クロスフェード（フェードイン）適用条件の限定**
    *   NOWの画像をフェードインさせる処理は、「画像が存在せずガラス板で待機していた状態から、非同期ロードが完了して画像が到着した瞬間」にのみ適用するように修正する。
    *   既に画像が手元にあり、ガラス板を経由せずに描画されていた場合は、着地時であってもフェードイン処理（アルファ値の0からの加算）を行わず、常に不透明度 `1.0f` でそのまま正規画像を描画し続けるようにする。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳密な限定**: 本タスクは `Widget_TrackInfo.cpp` 内の描画条件（ガラス板への差し替え判定とアルファ値の計算）の修正のみに限定する。スクロール距離の計算やテキスト描画のロジックには触れないこと。
-----------------------------------------------------------------------------------------

### 作業指示書 REQ: Phase 21-8 Task 5 & Hotfix : 先読み画像引継ぎと背景アート同期
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260718_1445_RES_Phase21-8_TrackDrumCore.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはトラックドラムコアエンジン実装のTask 5および画像引継ぎバグの修正（Hotfix）である。直ちに以下の【実装要件】に従ってコードの修正を実行すること。
2. 作業完了後、既存の作業レポートの「タスク5」のチェックボックスを完了 `[x]` にし、末尾のHotfix項目に「手動切り替え時の先読み画像引継ぎ」の対応内容を追記すること。
3. チャットにて「背景アート同期と先読み画像引継ぎ(Phase 21-8 Task 5 & Hotfix)が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
*   **要件1: 手動切り替え時の先読み画像（Prefetch）の完全引継ぎ (Hotfix)**
    *   `Application_Playback.cpp` 等の手動切り替え操作（`PlayCurrentTrack` やメディアコマンド）において、既に `PrefetchNextTrack` スレッドによって次曲の画像のロードが完了している場合（`m_isPrefetchReady` 等）は、画像を捨てて `nullptr` でリセットするのではなく、その先読み画像（`m_prefetchedAlbumArt`）を NOWの画像として `m_renderer.SetAlbumArt()` へ正しく引き継ぐようにロジックを修正する。
*   **要件2: 背景アートのバケツリレーとクロスフェード同期 (Task 5)**
    *   `Renderer_Draw.cpp` の `DrawBackground` にて、トラックドラムのアニメーション中（`m_isDrumAnimating`）は、OLDの背景画像とNOWの背景画像をドラムの進行度に基づいてクロスフェード（またはスライド）させる処理を実装し、左下UIと完全に同期させる。
    *   NOW画像が未ロード（`nullptr`）の場合、一瞬だけデフォルトのプレースホルダー画像が表示されてしまうのを防ぐため、プレースホルダーではなく「黒（暗転）」や「OLD画像を維持して徐々に暗くする」などのフェード処理にフォールバックさせ、視覚的破綻を防止する。

#### 【絶対遵守ルール (Constraints)】
*   **スコープの厳守**: 今回は画像引継ぎと背景アートの同期のみを行う。Task 6（シャッフル状態変更時の安全遷移）やマウスホイール操作等の実装は絶対に行わないこと。
