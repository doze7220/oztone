### REQ: Phase 12-3 Hotfix: 左配置プレイリスト展開時のホバー判定バグ修正 (実装実行)

以下のプロジェクトルールと開発資料を熟読すること。
* D:\ozlab\oztone\PROJECT_CONSTITUTION.md
* D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
事前の計画立案は省略し、即座に以下の手順でコードの修正を実行すること。
1. 以下の【実装要件】に従い、対象ファイルのコードを修正すること。
2. 修正が完了したら、チャットにて「プレイリストホバー判定のHotfixが完了しました。動作確認をお願いします」と報告すること。

#### 【実装要件】
本タスクの目的は、プレイリスト左配置時に、ロゴ・メニュー領域にマウスが入るとプレイリストが意図せず閉じてしまうバグを修正することである。

* **要件1: プレイリスト展開中のロゴ領域除外の無効化 (Window)**
  * `src/Window.cpp` の `Window::IsInPlaylistRegion` において、Phase 12-2 で追加された「ロゴ・拡張メニュー領域との干渉排除（Y座標がロゴ領域以下の場合は false を返す処理）」を修正する。
  * プレイリストが **展開中（`m_isPlaylistHovered == true`）の場合は、このロゴ・メニュー領域の除外ロジックをスキップ（無効化）** し、プレイリストのホバー判定を優先するように条件文を改修すること。
  * ※ Phase 8-8 で実装された、画面下部（シークバー等）の干渉回避用Y座標制限が `m_isPlaylistHovered` によって無効化されているロジック（`m_isPlaylistHovered || (logicalY < ...)`）と同様のアプローチをとること。

#### 【対象ファイル】
* `src/Window.cpp`
