【目的】
Phase 8-5 完了：ウィンドウの限界サイズ（最小サイズ）をハードコードし、OSレベルでそれ以上縮まらないように制限をかけます。

【実装要件】
1. 限界サイズ制限の追加 (Window.cpp)
- WindowProc 内で `WM_GETMINMAXINFO` メッセージを捕捉する処理を追加してください。
- `lParam` を `MINMAXINFO*` にキャストしてください。
- ウィンドウの現在のDPIを `GetDpiForWindow(hwnd)` で取得してください。（※初期化前などで取得できない場合はデフォルトの96を使用する等の安全策を入れてください）
- 論理限界サイズ (Width: 495, Height: 260) にDPIスケーリングを適用した物理ピクセルサイズを計算してください。（例: `MulDiv(495, dpi, 96)`）
- 計算した物理サイズを `pMinMaxInfo->ptMinTrackSize.x` および `ptMinTrackSize.y` に代入し、0 を返して処理を完了してください。

プロジェクト憲法に従い、既存のアーキテクチャを崩さず、実装完了後は変更点と懸念点をまとめた実装レポートを出力してください。
