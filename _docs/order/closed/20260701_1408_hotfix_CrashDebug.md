【AIへの指示（Hotfix 9: 起動時クラッシュの修正）】

Phase 7で実装したビジュアライザにおいて、起動直後（No Track状態）に空のスペクトルデータへアクセスしてしまい、Debugビルドでアサーションエラー（強制終了）が発生する問題に対処します。

以下の要件に従って修正してください。

**【実装要件】**
1. **Visualizer.cpp の修正**:
   - `Visualizer::DrawPrismBeat` および `Visualizer::DrawCircleParticle` メソッドの描画ロジックの最上段（先頭）に、ガード句を追加してください。
   - 引数として渡された `spectrum` が空であるか（`spectrum.empty()`）、あるいは十分なサイズを持っていない場合（例: `spectrum.size() < 2` など）は、描画処理を行わずに即座に `return;` して関数を抜けるようにしてください。

**【事後処理】**
ビルドを行い、曲を一切読み込んでいない起動直後の「No Track」状態でも、アプリケーションがクラッシュせずに正常に起動し待機できることを確認して報告してください。
