### 作業指示書 REQ: Phase 20-12: Rendererの完全解体（ファイル分割）
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
2. 以下の【実装要件】を満たすための高度なアーキテクチャ設計と実装計画を、兼作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase20-12_RendererFileSplit.md）として新規作成すること。
3. レポートのフォーマットは、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠し、細かなタスクリストを含めること。
4. チャットにて「計画書の作成が完了しました。タスクを実行するための新しいチャットへ移行してください」と報告すること。

#### 【実装要件】
現在、肥大化している `src/Renderer.cpp` について、クラスの定義（`Renderer.h`）や依存関係は一切変更せず、AI-IDEのコンテキスト節約および可読性向上のため、実装のみを責務別の `.cpp` ファイルに物理分割する。

*   **要件1: 初期化とリソース管理の分離 (`Renderer_Initialize.cpp`)**
    *   `Initialize`、`Resize`、`ReloadResources`、および各種リソース生成処理（DirectX、WIC、DirectWrite、ブラシ生成など）を移行する。
*   **要件2: 画像デコード処理の分離 (`Renderer_Image.cpp`)**
    *   `LoadBitmapResource`、`LoadBitmapFromMemory` などの画像読み込みユーティリティ処理を移行する。
*   **要件3: 状態更新処理の分離 (`Renderer_Update.cpp`)**
    *   `UpdateAnimation`、`UpdateTextLayouts` などの、毎フレームの描画前状態更新処理を移行する。
*   **要件4: 描画ループ処理の分離 (`Renderer_Draw.cpp`)**
    *   `Render`、`DrawBackground`、`DrawVisualizer` などの純粋な描画処理を移行する。
*   **要件5: コンテキスト構築の分離 (`Renderer_Context.cpp`)**
    *   `WidgetContext` の構築を行う処理（`BuildAnimationContext` や `BuildLayoutContext` 等、関数化されているあるいは切り出せる場合）を `Renderer_Context.cpp` へ移行する。※セッター等の細かなメソッドは無理に別ファイルへ分割せず本体に残してよい。
*   **要件6: 大元ファイルの整理と CMakeLists の更新**
    *   大元の `Renderer.cpp` には、責務分類が難しい小規模ユーティリティおよびコンストラクタ・デストラクタ、各種セッター等のみを残し、巨大な責務単位のみを分割する。
    *   新設したすべての `.cpp` ファイルを `CMakeLists.txt` に追加し、ビルドが通る状態を構築する計画を立てる。
*   **要件7: #include の最適化と依存関係の維持**
    *   分割後の各 `.cpp` ファイルは、必要なヘッダのみをインクルードし、不要な依存関係を持ち込まないこと。

#### 【作業終了後】
1. 作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase20-12_RendererFileSplit.md）に、詳細作業内容を記載する（タスクリストに含める）こと。
2. D:\ozlab\oztone\PROJECT_ARCHITECTURE.md を確認し、ファイル分割に関する記述を更新する（タスクリストに含める）こと。

#### 【絶対遵守ルール (Constraints)】
*   **ヘッダの維持**: `Renderer.h` のクラス定義（メンバ変数、メソッドのシグネチャ）は極力変更せず、実装の物理分割に専念すること。
*   **機能変更の禁止**: 描画順序やロジック自体は一切変更しないこと。

--------------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-12: Rendererの完全解体 (実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2301_RES_Phase20-12_RendererFileSplit.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-12（Rendererのファイル分割）の「実装実行」である。事前の計画書（RES）に記載されたすべてのタスク（タスク1〜タスク8）を一気通貫で実行すること。
2. 作業完了後、既存の作業レポート（20260713_2301_RES_Phase20-12_RendererFileSplit.md）のすべてのチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「Rendererの完全解体(Phase 20-12)がすべて完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
`src/Renderer.cpp` に同居している実装を、クラス定義（`Renderer.h`）を変更することなく、以下の物理ファイルへ完全に分割する。

*   **要件1: 初期化とリソース管理 (`src/Renderer_Initialize.cpp`)**
    *   `Initialize`, `Resize`, `ReloadResources` および各種リソース生成処理を移行する。
*   **要件2: 画像デコード (`src/Renderer_Image.cpp`)**
    *   `LoadBitmapResource`, `LoadBitmapFromMemory` などを移行する。
*   **要件3: 状態更新 (`src/Renderer_Update.cpp`)**
    *   `UpdateAnimation`, `UpdateTextLayouts` を移行する。
*   **要件4: 描画ループ (`src/Renderer_Draw.cpp`)**
    *   `Render`, `DrawBackground`, `DrawVisualizer` などを移行する。
*   **要件5: コンテキスト構築 (`src/Renderer_Context.cpp`)**
    *   `WidgetContext` の構築処理など（分離可能な場合）を移行する。
*   **要件6: 大元ファイルの整理 (`src/Renderer.cpp`)**
    *   コンストラクタ、デストラクタ、各種セッター、責務分類が難しい小規模ユーティリティのみを残し、巨大な責務をパージする。
*   **要件7: ビルド設定の更新 (`CMakeLists.txt`)**
    *   新設したすべての `.cpp` ファイルをソースリストに追加する。
*   **要件8: ドキュメントの更新 (`PROJECT_ARCHITECTURE.md`)**
    *   Rendererクラスのアーキテクチャ説明に、実装が物理分割された旨を追記する。

#### 【絶対遵守ルール (Constraints)】
*   **ヘッダの維持**: `Renderer.h` のクラス定義（メンバ変数、メソッドのシグネチャ）は極力変更せず、実装の物理分割に専念すること。
*   **機能変更の禁止**: 描画順序やロジック自体は一切変更しないこと。
*   **#include の最適化**: 分割後の各 `.cpp` ファイルは、必要なヘッダのみをインクルードし、不要な依存関係を持ち込まないこと。
--------------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-12 詳細レポートの事後作成
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\_docs\logs\20260713_2301_RES_Phase20-12_RendererFileSplit.md

#### 【作業手順（厳守事項）】
1. 現在の `src/Renderer*.cpp` ファイル群と `CMakeLists.txt` の状況をスキャン・解析すること。
2. 既存の `20260713_2301_RES_Phase20-12_RendererFileSplit.md` の「4. 詳細作業内容」の各タスクの見出し（タスク1〜8）の下に、実際にどのような関数・実装がどのファイルへ移行・整理されたのか、事後報告として詳細な説明を追記してファイルをアップデートすること。
3. チャットにて「手抜きをして申し訳ありません！ 詳細レポートの事後作成が完了しました！」と報告すること。

#### 【絶対遵守ルール (Constraints)】
*   **コード変更の禁止**: 本作業はドキュメント（RESファイル）の更新のみである。C++のソースコード（.h / .cpp）やCMakeLists.txtには絶対に触れないこと。
--------------------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-12: Rendererアーキテクチャ資料の更新 (タスク8)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2301_RES_Phase20-12_RendererFileSplit.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-12の「タスク8（ドキュメント更新）」の実行である。直ちに以下の【実装要件】に従って `PROJECT_ARCHITECTURE.md` の修正を実行すること。
2. 作業完了後、チャットにて「アーキテクチャ資料の更新が完了しました。これにてRendererの解体作戦が真に完結しました！」と報告すること。

#### 【実装要件】
*   **要件1: Rendererクラスのアーキテクチャ記述の更新**
    *   `PROJECT_ARCHITECTURE.md` 内の「Renderer クラス (src/Renderer.h, cpp)」の説明セクションを更新する。
    *   巨大化していた `Renderer.cpp` が、AI-IDEのコンテキスト節約および可読性向上のため、クラス設計（.h）はそのままに、実装（.cpp）が以下のファイル群へ物理分割された旨をわかりやすく箇条書きで明記すること。
        *   `Renderer_Initialize.cpp`: 初期化、リソース生成、リサイズ処理
        *   `Renderer_Image.cpp`: WICを用いた画像デコード処理
        *   `Renderer_Update.cpp`: アニメーションおよびテキストレイアウトの状態更新処理
        *   `Renderer_Draw.cpp`: 純粋な描画ループ処理本体
        *   `Renderer_Context.cpp`: WidgetContextの構築処理
    *   `Renderer.cpp` 本体には、コンストラクタ・デストラクタやセッター等の最小限の窓口機能のみが残されている旨を記載すること。

#### 【絶対遵守ルール (Constraints)】
*   **コード変更の禁止**: 本作業は純粋なドキュメント（PROJECT_ARCHITECTURE.md）の更新のみである。C++のソースコード（.h / .cpp）やCMakeLists.txtには絶対に触れないこと。

--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------
