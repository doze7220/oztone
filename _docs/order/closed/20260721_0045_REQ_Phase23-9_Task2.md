### 作業指示書 REQ: Phase 23-9 Task 2 : ウィンドウ透過の維持とレイヤーグループ機構の定義
*  ルール: D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  開発資料:D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  実装計画書:D:\ozlab\oztone\_docs\logs\20260721_0037_RES_Phase23-9_BackgroundRefinement.md

#### 【作業手順（厳守事項）】
本プロンプトはPhase 23-9 Task 2: ウィンドウ透過の維持とレイヤーグループ機構の定義である。必ず以下の順序で作業を行うこと。
1. ルールおよび開発資料を熟読・把握すること。
2. 実装計画書（20260721_0037_RES_Phase23-9_BackgroundRefinement.md）を読み、今回の自分のスコープが「タスク2」のみであることを確認すること。
※タスク1（ConfigManagerの分離）は既にHotfixとして完了済みであるため、タスク2から開始すること。
3. 上記を確認した後、以下の【実装要件】に従って **「タスク2のみ」** の実装を開始し、ソースコードの修正を実行すること。絶対にタスク3（Rendererの描画対応）以降をフライングで実行しないこと。
4. 作業完了後、既存の作業レポートの「タスク2」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
5. チャットにて「レイヤーグループ機構の定義(Phase 23-9 Task 2)が完了しました。ビルド・動作確認をお願いします」と報告すること。

#### 【実装要件】
BackgroundManager が構築するレイヤー指示書を拡張し、透過ウィンドウを阻害する下敷きをパージしつつ、BgOpacity を適用するためのグループ化指示を追加する。

*   **要件1: レイヤータイプの拡張 (`src/BackgroundManager.h`)**
    *   `BackgroundLayerType` 列挙型に、`LayerGroupBegin` と `LayerGroupEnd` を追加する。

*   **要件2: 下敷き黒レイヤーの完全パージ (`src/BackgroundManager.cpp`)**
    *   `GetLayers()` メソッド内において、現在一番最初に追加されている「下敷き用の黒色カラーフィルレイヤー」の追加処理（`push_back`）を完全に削除する。

*   **要件3: レイヤーグループ化の指示構築 (`src/BackgroundManager.cpp`)**
    *   `GetLayers()` メソッド内において、背景要素（OLD画像、NEW画像）を追加する **直前** に、`type = LayerGroupBegin` かつ `opacity = m_config->GetBgOpacity()` となるレイヤーを `push_back` する。
    *   背景要素とポストエフェクト（ダークオーバーレイ）の追加が終わった **直後**（つまりリストの最後）に、`type = LayerGroupEnd` となるレイヤーを `push_back` する。

#### 【絶対遵守ルール (Constraints)】
*   **Rendererの変更禁止** : 本タスクでは `src/Renderer_Draw.cpp` など描画層のコードには一切触れないこと。今回は「指示書（レイヤー配列）の作り方」を変えるだけに留める。
