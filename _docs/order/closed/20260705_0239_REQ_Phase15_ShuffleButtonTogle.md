### REQ: シャッフルボタンの動的アイコン切り替えとトグル演出の廃止 (実装実行)

以下のプロジェクトルールと開発資料を熟読すること。
* D:\ozlab\oztone\PROJECT_CONSTITUTION.md
* D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【現状の仕様と修正目的】
現在、ロゴ拡張メニューのシャッフルボタンは「トグルボタン（ON/OFF）」として設定されており、OFF時には「🔀」アイコンがグレーアウトし赤い斜線が引かれる演出となっている。
しかし、シャッフルOFF時は「ストレート（順次）再生」という対等なモードであるため、斜線ではなくアイコン自体を「🔀 (シャッフル)」と「➡️ (ストレート)」で動的に切り替える仕様へと変更する。

#### 【作業手順（厳守事項）】
事前の計画立案は省略し、即座に以下の手順でコードの修正を実行すること。
1. 以下の【実装要件】に従い、対象ファイルのコードを的確に修正すること。
2. 修正が完了したら、チャットにて「シャッフルボタンの切り替え対応が完了しました。動作確認をお願いします」と報告すること。

#### 【実装要件】
* **要件1: メニュー定義の修正 (トグル演出の無効化)**
  * `src/Window.cpp` のコンストラクタ内にある `m_logoMenuItems` の初期化リストにおいて、シャッフルボタン（`ID_LOGO_SHUFFLE`）の定義を精査する。
  * `isToggle` の設定値を `false` に変更し、トグルOFF時の斜線描画が適用されないようにする。（他のパラメータは既存の構成に合わせること）
* **要件2: アイコンとテキストの動的切り替え (LogoMenuWidget::Draw)**
  * `src/Widgets.cpp` の `LogoMenuWidget::Draw` を修正する。
  * アイコンを描画するループ内において、対象アイテムの `commandId` がシャッフルボタンである場合、`config->GetShuffleMode()` を評価する。
  * `true` の場合はアイコンを `L"🔀"`、`false` の場合は `L"➡️"` に上書きして描画する。
  * アイコンホバー時の説明テキスト（上部に表示されるラベル）についても同様に分岐させ、`true` の場合は `L"再生モード：シャッフル"`、`false` の場合は `L"再生モード：プレイリスト登録順"`へ動的に切り替える処理を追加する。

#### 【作業終了後】
1. 以下の【実装要件】を満たす実装計画書を作業レポート（`D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_Phase15_ShuffleButtonTogle.md`）として新規作成する。
2. 原因と、詳細作業内容を記載する。
3. D:\ozlab\oztone\PROJECT_ARCHITECTURE.md を確認し、作業内容が記載に影響のある場合は修正を行う。

#### 【対象ファイル】
* `src/Window.cpp`
* `src/Widgets.cpp`
