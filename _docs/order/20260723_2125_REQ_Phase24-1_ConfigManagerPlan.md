##### 作業指示書 REQ: Phase 24-1: ConfigManager リファクタリング計画立案
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

###### 【作業手順（厳守事項）】
本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
1. ルール（PROJECT_CONSTITUTION.md）および開発資料（PROJECT_ARCHITECTURE.md）を熟読・把握すること。
2. 以下の【実装要件】を満たすための高度なアーキテクチャ設計と実装計画を立案し、AIがハルシネーションを起こさない適切なスコープを持つ細かなタスクリストを構築すること。
3. **【最重要】** 計画を立てる前に、必ず現在の `src/ConfigManager.h` および関連する `.cpp` ファイルのコードを読み込み・解析し、現状存在するすべての設定変数を把握した上で具体的な移行計画を立てること。
4. 計画を出力する前に内部で自己監査を行い、ルール・開発資料の責務違反がないか確認して計画を純化すること。（※監査プロセスのテキスト出力は不要）
5. 監査を通過した純度100%の計画書を、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠した作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase24-1_ConfigManagerPlan.md）として新規作成（出力）すること。
6. チャットにて「計画書の作成が完了しました。タスクを実行するための新しいチャットへ移行してください」と報告すること。

###### 【実装要件】
現在、`ConfigManager.h` に平べったく羅列されている変数群を構造体へとパッキングし、INIファイルのセクション（`[Audio]`, `[Layout_VolumeControl]` 等）と完全に1対1で対応する「1 Section = 1 Struct = 1 .cpp」のアーキテクチャへと刷新するための計画を立案する。

*   **要件1: ソースコード解析に基づくセクション分割**
    *  現在のコードを解析し、どの変数がどのINIセクションに属しているかを正確に把握した上で、各セクションに対応する構造体（例: `Config_Audio`）と物理ファイル（例: `ConfigManager_Audio.cpp`）の作成計画を立てること。
*   **要件2: タスクの細分化とハルシネーション対策の厳命**
    *  対象となる変数が極めて多いため、1回のタスクでのコピー＆ペースト量がAIのコンテキスト限界を超えないよう、タスクを安全な量（例：1タスクにつき1〜2セクションの移行）に細分化すること。
    *  計画書の冒頭等に「AIは1回のタスクで指定されたセクションのみを確実に行い、絶対に他のタスクをフライングしてはならない」という厳命を記載すること。
*   **要件3: ヘッダ登録の遅延化とメモ書き（安全装置）**
    *  各移行タスクの実行中には、大元である `ConfigManager.h` は一切書き換えず（旧変数も消さない）、代わりに各タスクの作業レポートに「最終的に `ConfigManager.h` に登録すべきインスタンス変数とゲッターのシグネチャ」をメモ（明記）として残させるルールを計画に組み込むこと。
*   **要件4: 最終タスクでの一斉結線とパージ**
    *  最後のタスクとして、各レポートに残されたメモを拾い上げて `ConfigManager.h` へ一気に登録（結線）し、旧変数・旧ゲッターをパージするタスクを用意すること。

###### 【タスクごとの終了後作業】
1. 作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase24-1_ConfigManagerPlan.md）に、詳細作業内容を記載する（タスクリストに含める）こと。

###### 【全タスク終了後の作業】
1. D:\ozlab\oztone\PROJECT_ARCHITECTURE.md を確認し、作業内容が記載に影響のある場合は修正を行う（タスクリストに含める）こと。

###### 【実装作業での絶対遵守ルール (Constraints)】
*   **AIキャパシティの保護**: 1回のタスクにおけるコードコピー量がハルシネーションを引き起こさないよう、物理ファイルの作成単位でタスクを適切に割ること。
*   **コード解析の必須化**: 想像や推測で変数名を記述せず、必ず実際のソースコードから読み取った真実のデータに基づく計画とすること。
   

-------------------------------------------------------------------------------

1つだけ【追加のアーキテクチャ要件】を指示する。今後のタスク実行時は、この追加要件を必ず遵守して物理ファイルを作成・移動すること。

### 【追加のアーキテクチャ要件】 Configディレクトリの新設
今回新設するすべてのセクションファイル（`Config_*.h`, `Config_*.cpp`）および、大元となる `ConfigManager.h`, `ConfigManager.cpp` は、新設する `src/Config/` ディレクトリ内に作成・移動させること。
*   **【例外絶対ルール】**: ただし、マスターデータである `src/ConfigManager_DefaultIni.h` だけは **絶対に移動させず、現在の `src/` 直下に残す** こと。新設したファイルから参照する際は `#include "../ConfigManager_DefaultIni.h"` とパスを遡って解決すること。
*   CMakeLists.txt を更新する際も、このディレクトリ構造（`src/Config/Config_*.cpp`）を正しく反映させること。

-------------------------------------------------------------------------------

