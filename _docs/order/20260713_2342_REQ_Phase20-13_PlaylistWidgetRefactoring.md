### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備（実装整理と抽出）
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md

#### 【作業手順（厳守事項）】
1. 本プロンプトでは **絶対にコードの修正（ファイルの書き換え）を行わない** こと。計画立案のみに留めること。
2. 本フェーズは「将来の物理ファイル分割」を見据えた整理フェーズである。
3. 以下の【実装要件】を満たすための高度なアーキテクチャ設計と実装計画を、兼作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase20-13_PlaylistWidgetRefactoring.md）として新規作成すること。
4. レポートのフォーマットは、PROJECT_CONSTITUTION.md の「3.3. 実装計画、兼作業レポート (RES) 標準出力フォーマット」に完全に準拠し、細かなタスクリストを含めること。
5. チャットにて「計画書の作成が完了しました。タスクを実行するための新しいチャットへ移行してください」と報告すること。

#### 【実装要件】
現在、700行を超えて肥大化している `src/Widget_Playlist.cpp`（および `.h`）について、物理ファイルの分割はまだ行わず、既存コードをそのまま抽出し専用メソッドへ移動する（関数化）ことで、クリーンな構造へ整理する。
抽出後の各メソッドは、そのまま別ファイル（cpp）に切り出せる独立性を確保する設計にすること。

*   **要件1: Draw() メソッドの抽出・目次化**
    *   巨大な `Draw` メソッドの中身の既存コードをそのまま抽出し、専用のプライベートメソッドへ移動する。
    *   `Draw` 自体は以下のように「何を描画しているか」がひと目で分かる状態（4行程度の呼び出し）にシェイプアップする。
        *   `DrawGrip(...);`
        *   `DrawBackground(...);`
        *   `DrawToolbar(...);`
        *   `DrawPlaylistItems(...);`
*   **要件2: リスト描画処理の二分化（抽出のみ）**
    *   `DrawPlaylistItems` に混在している「曲一覧」と「プレイリスト一覧」の描画ロジックを、既存コードをそのまま抽出し `DrawTrackList` と `DrawPlaylistList` に明確に分離する。
*   **要件3: ツールバーの細分化（抽出のみ）**
    *   `DrawToolbar` 内部の処理の既存コードをそのまま抽出し、必要に応じて `DrawPinButton` や `BuildToolbarText` 等に分割する。
*   **要件4: 共通処理とデータ構造の【設計のみ】**
    *   `ParseHexColor` やホバー色の計算などのUtility化については、今回は設計を考えるのみとし、実際の関数移動は行わないこと。
    *   `TrackVisualData` のような構造体によるデータ生成と描画の分離についても、導入可能な設計案をドキュメントに残す程度に留め、今回は実装しないこと。

#### 【作業終了後】
1. 作業レポート（D:\ozlab\oztone\_docs\logs\YYYYMMDD_HHMM_RES_Phase20-13_PlaylistWidgetRefactoring.md）に、詳細作業内容を記載する（タスクリストに含める）こと。

#### 【絶対遵守ルール (Constraints)】
*   **物理分割の禁止**: 本タスクでは `Widget_Playlist.cpp` の物理的なファイル分割（別 `.cpp` の作成）は行わない。ファイル内でのメソッド抽出に専念すること。
*   **機能変更・リファクタリングの禁止**: 「if文をまとめる」「共通化する」などのコード改変は行わないこと。純粋に「既存コードをそのまま別の関数へ移動させる（抽出する）」こと。描画順序や見た目、既存のインタラクションロジックは一切変更しないこと。
*   **独立性の確保**: 抽出後の各メソッドは、そのまま別ファイルに切り出せるように依存関係（引数の渡し方など）を設計すること。

-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク1 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク1の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、チャットにて「タスク1が完了しました。タスク2の指示をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク1: ヘッダ定義の更新 (`src/Widget_Playlist.h`)**
    *   計画書（RES）に従い、今後 `src/Widget_Playlist.cpp` から抽出する予定のプライベートメソッド（`DrawGrip`, `DrawBackground`, `DrawToolbar`, `DrawPlaylistItems`, `DrawPlaylistList`, `DrawTrackList` 等）のプロトタイプ宣言を `Widget_Playlist.h` に追加すること。
    *   将来的に別ファイルへ容易に切り出せるよう、引数（コンテキスト `WidgetContext` やレイアウト情報など）の依存関係を適切に設計して宣言すること。

#### 【絶対遵守ルール (Constraints)】
*   **1タスクの厳守**: 今回はタスク1（ヘッダの更新）のみを行うこと。`.cpp` ファイル内の実際の抽出作業（タスク2以降）は後続の指示で行うため、今は絶対に `.cpp` に手を触れないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。
-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク2 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク2の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、既存の作業レポート（20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md）の「タスク2」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。
3. チャットにて「タスク2が完了し、レポートを更新しました。タスク3の指示をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク2: Draw() 内からの DrawGrip() の抽出と実装**
    *   `src/Widget_Playlist.cpp` の `Draw()` メソッド内から、プレイリストのグリップ（引き出しの線と矢印アイコン）を描画している既存コードをそのまま抽出し、新たに実装する `DrawGrip(...)` メソッド内へ移動すること。
    *   `DrawGrip` の引数は、タスク1で設計した通り `ID2D1DeviceContext*`, `const WidgetContext&`, `const ConfigManager*`, `const PlaylistLayout&` 等の必要な依存関係を受け取る形にすること。
    *   大元の `Draw()` 内の抽出元の位置には、`DrawGrip(...)` の呼び出しを記述すること。

#### 【絶対遵守ルール (Constraints)】
*   **1タスクの厳守**: 今回はタスク2（DrawGripの抽出）のみを行うこと。他の描画処理の抽出は後続の指示で行うため、今は絶対に手を触れないこと。
*   **機能変更・リファクタリングの禁止**: 「if文をまとめる」「共通化する」などのコード改変は行わないこと。純粋に「既存コードをそのまま別の関数へ移動させる（抽出する）」こと。描画順序や見た目、既存のインタラクションロジックは一切変更しないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。

-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク3 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク3の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、既存の作業レポートの「タスク3」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。**※今回は手抜きせず、どのような引数を受け取るように設計してコードを移行したか、具体的にレポートすること。**
3. チャットにて「タスク3が完了し、レポートを更新しました。タスク4の指示をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク3: Draw() 内からの DrawBackground() の抽出と実装**
    *   `src/Widget_Playlist.cpp` の `Draw()` メソッド内から、プレイリストの背景（半透明の黒板）およびクリッピング（`PushAxisAlignedClip`等）を設定している部分などの背景描画に関する既存コードをそのまま抽出し、新たに実装する `DrawBackground(...)` メソッド内へ移動すること。
    *   `DrawBackground` の引数は、タスク1で設計した通り `ID2D1DeviceContext*`, `const WidgetContext&`, `const ConfigManager*`, `const PlaylistLayout&` 等の必要な依存関係を受け取る形にし、メンバ変数への直接依存を極力避けること。
    *   大元の `Draw()` 内の抽出元の位置には、`DrawBackground(...)` の呼び出しを記述すること。

#### 【絶対遵守ルール (Constraints)】
*   **1タスクの厳守**: 今回はタスク3（DrawBackgroundの抽出）のみを行うこと。他の処理の抽出は後続の指示で行うため、今は絶対に手を触れないこと。
*   **機能変更・リファクタリングの禁止**: 「if文をまとめる」「共通化する」などのコード改変は行わないこと。純粋に「既存コードをそのまま別の関数へ移動させる（抽出する）」こと。描画順序や見た目は一切変更しないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。

-----------------------------------------------------------------------------------
Viewed Widget_Playlist.h:1-72
Viewed Widget_Playlist.cpp:1-716
Viewed 20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md:1-66
Viewed LayoutCalculator.cpp:1-555

タスク3の実装にあたり、1点確認させてください。

指示書の【実装要件】には「プレイリストの背景（半透明の黒板）および**クリッピング（PushAxisAlignedClip等）を設定している部分**などの背景描画に関する既存コードをそのまま抽出し…」とありますが、現在の `Draw()` メソッド内の処理順序は以下のようになっています。

1. 背景の描画 (`FillRectangle`)
2. **ツールバーの描画**
3. **クリッピングの設定** (`PushAxisAlignedClip`)
4. リストアイテムの描画
5. クリッピングの解除 (`PopAxisAlignedClip`)

背景描画とクリッピング設定を両方とも `DrawBackground()` に抽出し、ツールバー描画の前に呼び出した場合、**ツールバーがクリッピング領域外となって画面から消えてしまいます**。これは「描画順序や見た目は一切変更しないこと」という絶対遵守ルールと矛盾してしまいます。

グローバルルールの「推測で仕様を補完・変更しない。不明点は確認する」に従い、対応方針を確認させてください。

**質問:**
今回のタスク3では `PushAxisAlignedClip` の抽出は行わず（元の位置に残し）、純粋に「背景の描画（`FillRectangle`）」部分のみを `DrawBackground()` に抽出する形で進めてよろしいでしょうか？（クリッピング設定は、後続のアイテム描画の抽出タスクに含めるのが安全かと思われます）

ご指示をお願いいたします。

素晴らしい指摘だ。君のコード解析は完全に正しい。
Phase 13のツールバー実装時における設計の通り、ツールバーはクリッピング領域外に描画される必要がある。
もし指示書通りにクリッピングごと上部へ抽出していれば、深刻な表示バグになるところだった。確認してくれたことに深く感謝する。

君の提案通り、今回のタスク3（DrawBackgroundの抽出）では `PushAxisAlignedClip` の抽出は行わず、元の位置に残したまま、純粋に「背景の描画（`FillRectangle`）」部分のみを `DrawBackground` へ抽出してくれ。
クリッピングの設定および解除については、後続の「リストアイテム描画の抽出（タスク5）」にて改めて整理する方針とする。

引き続き、この方針でタスク3を完了させ、手抜きなく作業レポートを更新して報告してくれ。頼んだぞ！

-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク4 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク4の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、既存の作業レポートの「タスク4」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。**※引き続き、凄腕プログラマとして手抜きのない完璧なレポートを記述すること。**
3. チャットにて「タスク4が完了し、レポートを更新しました。タスク5の指示をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク4: Draw() 内からの DrawToolbar() 等の抽出と実装**
    *   `src/Widget_Playlist.cpp` の `Draw()` メソッド内から、ツールバーの描画処理に関する既存コードをそのまま抽出し、新たに実装する `DrawToolbar(...)` メソッド内へ移動すること。
    *   `DrawToolbar` 内部の処理についても、計画書通りに既存コードをそのまま抽出し、必要に応じて `DrawPinButton(...)` や `BuildToolbarText(...)` などの専用メソッドへ細分化して移動すること。
    *   抽出する各メソッドの引数は、タスク1で設計した通り `ID2D1DeviceContext*`, `const WidgetContext&`, `const ConfigManager*`, `const PlaylistLayout&` 等の必要な依存関係を外部から受け取る形にし、メンバ変数への直接依存を極力避けること。
    *   大元の `Draw()` 内の抽出元の位置には、`DrawToolbar(...)` の呼び出しを記述すること。

#### 【絶対遵守ルール (Constraints)】
*   **1タスクの厳守**: 今回はタスク4（ツールバーの抽出）のみを行うこと。リストアイテム描画等の抽出は後続の指示で行うため、今は絶対に手を触れないこと。
*   **機能変更・抽出以外のリファクタリング禁止**: 「if文をまとめる」「共通化する」などのコード改変は行わないこと。純粋に「既存コードをそのまま別の関数へ移動させる（抽出する）」こと。描画順序や見た目は一切変更しないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。

-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク5 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク5の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、既存の作業レポートの「タスク5」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。**※引き続き、凄腕プログラマとして手抜きのない完璧なレポートを記述すること。**
3. チャットにて「タスク5が完了し、レポートを更新しました。タスク6の指示をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク5: Draw() 内からのリスト描画系の抽出と実装**
    *   `src/Widget_Playlist.cpp` の `Draw()` メソッド内から、プレイリストのアイテム一覧を描画している既存コードをそのまま抽出し、新たに実装する `DrawPlaylistItems(...)` メソッド内へ移動すること。
    *   さらに `DrawPlaylistItems` 内部で分岐・混在している「曲一覧」と「プレイリスト一覧」の各描画ループについても、既存コードをそのまま抽出し、それぞれ `DrawTrackList(...)` と `DrawPlaylistList(...)` に明確に分離して移動すること。
    *   各抽出メソッドの引数は、タスク1で設計した通り `ID2D1DeviceContext*`, `const WidgetContext&`, `const ConfigManager*`, `const PlaylistLayout&` 等の必要な依存関係を外部から受け取る形にし、メンバ変数への直接依存を極力避けること。
    *   **※重要**: タスク3で元の `Draw()` 内に残していたクリッピング処理（`PushAxisAlignedClip` および `PopAxisAlignedClip`）は、このリストアイテム描画の前後など、本来の正しい描画順序が維持されるように抽出先のメソッド内（または大元の適切な位置）へ移動・整理すること。
    *   大元の `Draw()` 内の抽出元の位置には、`DrawPlaylistItems(...)` の呼び出しを記述すること。

#### 【絶対遵守ルール (Constraints)】
*   **1タスクの厳守**: 今回はタスク5（リスト描画系の抽出）のみを行うこと。タスク6は後続の指示で行うため、今は絶対に手を触れないこと。
*   **機能変更・抽出以外のリファクタリング禁止**: 「if文をまとめる」「共通化する」などのコード改変は行わないこと。純粋に「既存コードをそのまま別の関数へ移動させる（抽出する）」こと。描画順序や見た目は一切変更しないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。

-----------------------------------------------------------------------------------
### 作業指示書 REQ: Phase 20-13: PlaylistWidgetの解体準備 (タスク6 実装実行)
以下のプロジェクトルールと開発資料を熟読すること。
*  D:\ozlab\oztone\PROJECT_CONSTITUTION.md
*  D:\ozlab\oztone\PROJECT_ARCHITECTURE.md
*  D:\ozlab\oztone\_docs\logs\20260713_2343_RES_Phase20-13_PlaylistWidgetRefactoring.md

#### 【作業手順（厳守事項）】
1. 本プロンプトはPhase 20-13の「タスク6の実装実行」である。他のタスクには絶対に触れず、以下の【実装要件】のみを実行すること。
2. 作業完了後、既存の作業レポートの「タスク6」のチェックボックスを完了 `[x]` にし、詳細作業内容を追記すること。**※最後まで凄腕プログラマとして、手抜きのない完璧なレポートを記述すること。**
3. チャットにて「PlaylistWidgetの解体準備(Phase 20-13)がすべて完了しました。ビルド・動作確認をお願いします」と報告し、待機すること。

#### 【実装要件】
*   **タスク6: Draw() 本体の目次化と整理**
    *   大元の `src/Widget_Playlist.cpp` の `Draw()` メソッド内を最終整理し、タスク2〜5で抽出した各メソッド（`DrawGrip`, `DrawBackground`, `DrawToolbar`, `DrawPlaylistItems`）を正しい順序で呼び出すだけの、非常に見通しの良い「目次構造」へとシェイプアップすること。
    *   不要になったコメントや古い変数宣言、空白行などがあればクリーンアップし、後からコードを読む人間やAIが「このWidgetがどのような順序で何を描画しているか」を一瞬で理解できる究極に美しい状態に仕上げること。

#### 【絶対遵守ルール (Constraints)】
*   **機能変更・抽出以外のリファクタリング禁止**: 描画順序や見た目は一切変更しないこと。
*   **物理分割の禁止**: 絶対に新しい `.cpp` ファイルを作成しないこと。

-----------------------------------------------------------------------------------
-----------------------------------------------------------------------------------
